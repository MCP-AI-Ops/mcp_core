# MCP Core 아키텍처 및 자동화 흐름

## 📋 목차
1. [프로젝트 개요](#1-프로젝트-개요)
2. [전체 자동화 흐름](#2-전체-자동화-흐름)
3. [디렉터리 구조 및 역할](#3-디렉터리-구조-및-역할)
4. [자동화 시나리오](#4-자동화-시나리오)
5. [API 계약 (Contract)](#5-api-계약-contract)
6. [데이터 파이프라인](#6-데이터-파이프라인)
7. [예외 처리 및 폴백](#7-예외-처리-및-폴백)
8. [배포 및 실행](#8-배포-및-실행)

---

## 1. 프로젝트 개요

### 핵심 미션
**사용자가 GitHub 저장소 주소만 입력하면 → 자동으로 리소스 예측 → 배포 가능한 스펙 제안**

### 자동화 목표
```
사용자 입력: "https://github.com/fastapi/fastapi 분석해줘"
                    ↓
         [완전 자동 파이프라인]
                    ↓
출력: "추천 인스턴스: medium (4 CPU, 8GB)
      예상 비용: $2.80/day
      향후 24시간 예측: [그래프/테이블]
      이상 징후: 없음 ✅"
```

**중요**: 사용자는 어떤 모델을 쓰는지, 어떤 파라미터가 필요한지 전혀 알 필요 없음. 오직 **레포 주소만** 제공.

---

## 2. 전체 자동화 흐름

### 2.1 End-to-End 파이프라인 (올바른 구조)

```
┌─────────────────────────────────────────────────────────────────┐
│                    사용자 (웹 프론트엔드)                          │
│                                                                   │
│  입력: GitHub URL                                                 │
│  예: "https://github.com/fastapi/fastapi"                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP POST /api/analyze
                            │ {"github_url": "..."}
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│              backend_api/main.py                                 │
│                (Backend API Gateway)                             │
│                                                                   │
│  [자동화 단계 1: GitHub 분석 - 백엔드에서 직접 실행]               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ GitHubAnalyzer.analyze_repository()                      │   │
│  │                                                            │   │
│  │ 1. GitHub API 호출 (PyGithub)                            │   │
│  │    - repo = g.get_repo("fastapi/fastapi")                │   │
│  │    - stars, forks, language, README 수집                 │   │
│  │                                                            │   │
│  │ 2. 자동 분석 엔진 실행                                      │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ detect_service_type()                       │        │   │
│  │    │ - README + 언어 키워드 매칭                  │        │   │
│  │    │ - "fastapi" 발견 → "web"                    │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ detect_time_slot()                          │        │   │
│  │    │ - 최근 커밋 시간 분석                        │        │   │
│  │    │ - 14시 커밋 → "peak"                        │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ estimate_users()                            │        │   │
│  │    │ - stars × 0.05 + forks × 1.5                │        │   │
│  │    │ - 91,825 × 0.05 + 8,212 × 1.5 = 12,483명   │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ estimate_cpu_memory()                       │        │   │
│  │    │ - 언어: Python → 2 CPU, 4GB                 │        │   │
│  │    │ - 타입: web → memory × 1.5                  │        │   │
│  │    │ - 규모: >50k stars → both × 4               │        │   │
│  │    │ - 결과: 4 CPU, 8GB                          │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │                                                            │   │
│  │ 3. MCPContext 자동 생성                                    │   │
│  │    {                                                       │   │
│  │      "service_type": "web",                               │   │
│  │      "current_users": 12483,                              │   │
│  │      "time_slot": "peak",                                 │   │
│  │      "cpu": 4,                                            │   │
│  │      "memory": 8192,                                      │   │
│  │      "runtime_env": "prod"                                │   │
│  │    }                                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  [자동화 단계 2: MCP Core 호출]                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ MCPCoreClient.request_plans()                             │   │
│  │                                                            │   │
│  │ HTTP POST http://app:8000/plans                           │   │
│  │ {                                                          │   │
│  │   "github_url": "fastapi/fastapi",                        │   │
│  │   "metric_name": "total_events",                          │   │
│  │   "context": { ... }  // 위에서 생성한 MCPContext         │   │
│  │ }                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP Request (내부 통신)
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                 app/routes/plans.py                              │
│                   (MCP Core FastAPI)                             │
│                                                                   │
│  [자동화 단계 3: 예측 파이프라인] (이전과 동일)                    │
│  1. Context 검증                                                  │
│  2. 라우팅 (모델 선택)                                            │
│  3. LSTM 예측 (실패 시 Baseline 폴백)                            │
│  4. 정책 후처리 + 이상탐지                                        │
│  5. Discord 알림 (이상 시, 비차단)                                │
│  6. 추천 계산 (flavor, cost)                                     │
│  7. DB 저장 (선택적)                                              │
│                                                                   │
│  PlansResponse {                                                 │
│    prediction: {24 points, is_anomaly, ...},                     │
│    recommended_flavor: "medium",                                 │
│    expected_cost_per_day: 2.80                                   │
│  }                                                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP 200 OK (JSON)
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│              backend_api/main.py                                 │
│                                                                   │
│  [자동화 단계 4: 응답 포매팅]                                      │
│  AnalyzeResponse {                                               │
│    success: true,                                                │
│    repository: {name, stars, language, ...},                     │
│    predictions: {24h_forecast, is_anomaly, ...},                 │
│    recommendations: {instance_type, cpu, memory},                │
│    cost_estimate: {daily_usd, monthly_usd}                       │
│  }                                                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP 200 OK (JSON)
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                    사용자 (웹 프론트엔드)                          │
│                                                                   │
│  ┌─────────────────────────────────────────────────────┐        │
│  │ 📊 분석 완료!                                        │        │
│  │                                                      │        │
│  │ 저장소: fastapi/fastapi                              │        │
│  │ 언어: Python                                         │        │
│  │ Stars: 91,825 ⭐                                     │        │
│  │                                                      │        │
│  │ 추천 인스턴스: medium                                │        │
│  │ 스펙: 4 vCPU, 8GB RAM                                │        │
│  │ 예상 비용: $2.80/day ($84/month)                     │        │
│  │                                                      │        │
│  │ [24시간 예측 그래프]                                 │        │
│  │ [배포 버튼]                                          │        │
│  └─────────────────────────────────────────────────────┘        │
└───────────────────────────────────────────────────────────────────┘
```

### 2.2 완전 자동화 핵심 포인트

**사용자가 제공하는 것**: 오직 GitHub URL만  
**백엔드가 자동으로 하는 것**:
1. ✅ GitHub 메타데이터 수집 (PyGithub)
2. ✅ 서비스 타입 추론 (web/api/worker/data)
3. ✅ 활성 시간대 감지 (peak/normal/low)
4. ✅ 사용자 수 계산 (Stars/Forks 기반)
5. ✅ CPU/Memory 추정 (다단계 로직)
6. ✅ MCPContext 생성 (필요한 모든 필드)
7. ✅ MCP Core /plans 호출
8. ✅ 모델 선택 (라우팅 규칙 기반)
9. ✅ 데이터 로드 및 전처리
10. ✅ 예측 실행 (LSTM → 실패 시 Baseline)
11. ✅ 이상탐지 및 알림
12. ✅ 비용/스펙 추천
13. ✅ 결과 포매팅 및 반환

**사용자가 절대 신경쓰지 않는 것**:
- MCP가 무엇인지
- 어떤 모델이 사용되는지
- 어떤 파라미터가 필요한지
- 데이터가 어디서 오는지
- 스케일러/정규화 같은 ML 디테일
- DB 저장 여부
- 이상탐지 로직

### 2.3 두 가지 사용 경로

#### 경로 1: 프로덕션 사용자 (Frontend → Backend API)
```
사용자 → 프론트엔드 → POST /api/analyze → backend_api/main.py
                                             ↓
                                    GitHub 분석 + MCPContext 생성
                                             ↓
                                    POST /plans → app:8000
                                             ↓
                                    예측 + 추천 결과 반환
```

#### 경로 2: 개발자 도구 (Claude Desktop → MCP Stdio)
```
Claude Desktop → stdio → server_production.py → app:8000
                            ↓
                   자연어 → MCPContext 변환 (수동 입력)
                            ↓
                   POST /plans → 예측 결과
```

**중요**: 
- 경로 1 (Backend API)이 **실제 서비스**에 사용됩니다
- 경로 2 (MCP Stdio)는 **개발자/테스트용**입니다
- 사용자는 경로 1만 보게 됩니다 (GitHub URL 입력 → 결과 확인)

## 3. 아키텍처 구성 요소

### 3.1 서비스 구조 (Docker Compose)

```yaml
services:
  mysql:
    - 포트: 3306
    - 역할: 예측 결과 저장 (선택적)
    
  app:  # MCP Core
    - 포트: 8000
    - 엔드포인트: /plans, /deploy, /destroy, /status
    - 역할: 예측 엔진 (LSTM + Baseline)
    
  backend:  # Backend API Gateway
    - 포트: 8001
    - 엔드포인트: /api/analyze
    - 역할: GitHub 분석 + 자동화 오케스트레이션
    - 의존성: app 서비스 (MCP Core)
```

### 3.2 Backend API 상세 (`backend_api/main.py`)

#### 핵심 엔드포인트

**POST /api/analyze**
- 입력: `{"github_url": "https://github.com/owner/repo"}`
- 출력: 전체 분석 결과 (저장소 정보 + 예측 + 추천 + 비용)
- 자동화: GitHub 분석 → MCPContext 생성 → /plans 호출 → 포매팅

**GET /health**
- MCP Core 연결 상태 확인

#### GitHubAnalyzer 자동화 로직

```python
def analyze_repository(github_url: str) -> dict:
    """GitHub URL → 완전한 분석 결과"""
    
    # 1. GitHub API 호출
    repo = g.get_repo("owner/repo")
    
    # 2. 서비스 타입 자동 감지
    service_type = _detect_service_type(repo)
    # README 키워드: fastapi/flask → web
    #                 celery/rq → worker
    #                 postgresql/mongodb → data
    
    # 3. 시간대 자동 감지
    time_slot = _detect_time_slot(repo)
    # 최근 커밋 시간: 9-18시 → peak
    #                19-23시 → normal
    #                0-8시 → low
    
    # 4. 사용자 수 자동 계산
    users = _estimate_users(repo)
    # stars × 0.05 + forks × 1.5
    
    # 5. 리소스 자동 추정
    cpu, memory = _estimate_cpu_memory(repo)
    # 언어별 베이스라인 → 서비스 타입 배수 → Stars 스케일링
    
    return {
        "service_type": service_type,
        "current_users": users,
        "time_slot": time_slot,
        "cpu": cpu,
        "memory": memory,
        "runtime_env": "prod"
    }
```

### 3.3 MCP Core 상세 (`app/routes/plans.py`)

**기존 계약 유지 - Backend API가 호출하는 내부 엔드포인트**

```python
POST /plans
{
  "github_url": "fastapi/fastapi",
  "metric_name": "total_events",
  "context": {
    "service_type": "web",        # Backend에서 자동 감지
    "current_users": 12483,       # Backend에서 자동 계산
    "time_slot": "peak",          # Backend에서 자동 감지
    "cpu": 4,                     # Backend에서 자동 추정
    "memory": 8192,               # Backend에서 자동 추정
    "runtime_env": "prod"
  }
}
```

응답:
```python
{
  "prediction": {
    "hours": [0,1,2,...,23],
    "values": [120,135,...,98],
    "predictor_used": "lstm",
    "is_anomaly": false,
    "anomaly_score": 0.32
  },
  "recommended_flavor": "medium",
  "expected_cost_per_day": 2.80,
  "metadata": {
    "model_version": "1.0",
    "prediction_confidence": 0.89
  }
}
```


```
┌─────────────────────────────────────────────────────────────────┐
│                      사용자 (Claude Desktop)                      │
│                                                                   │
│  입력: "https://github.com/fastapi/fastapi 분석해줘"              │
│  또는: "@github-analyzer analyze https://github.com/..."         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ MCP Protocol (stdio)
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│            mcp_analyzer/server_production.py                     │
│                     (MCP Stdio Server)                           │
│                                                                   │
│  [자동화 단계 1: GitHub 분석]                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 1. GitHub API 호출                                        │   │
│  │    - PyGithub로 레포 메타데이터 수집                       │   │
│  │    - Stars, Forks, Language, README, 커밋 히스토리         │   │
│  │                                                            │   │
│  │ 2. 자동 분석 엔진                                           │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ detect_service_type()                       │        │   │
│  │    │ - README/언어 기반 서비스 타입 자동 추론     │        │   │
│  │    │ - 키워드: flask/django → "web"              │        │   │
│  │    │          celery/worker → "worker"           │        │   │
│  │    │          tensorflow/ml → "data"             │        │   │
│  │    │ - 결과: "api"                                │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ detect_time_slot()                          │        │   │
│  │    │ - 최근 커밋 시간대 분석                      │        │   │
│  │    │ - 9-18시 → "peak"                           │        │   │
│  │    │ - 22-6시 → "low"                            │        │   │
│  │    │ - 결과: "peak"                              │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ estimate_users()                            │        │   │
│  │    │ - 공식: stars × 0.05 + forks × 1.5          │        │   │
│  │    │ - FastAPI: 91,825 stars, 8,212 forks        │        │   │
│  │    │ - 결과: 12,483명                            │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │    ┌─────────────────────────────────────────────┐        │   │
│  │    │ estimate_cpu_memory()                       │        │   │
│  │    │ 1. 언어별 기본값                            │        │   │
│  │    │    - Python/JS: 2 CPU, 4GB                  │        │   │
│  │    │    - Java/Scala: 4 CPU, 8GB                 │        │   │
│  │    │    - Go/Rust: 2 CPU, 2GB                    │        │   │
│  │    │                                              │        │   │
│  │    │ 2. 서비스 타입 조정                          │        │   │
│  │    │    - web: memory × 1.5                      │        │   │
│  │    │    - api: cpu × 1.2                         │        │   │
│  │    │    - worker: cpu × 1.5                      │        │   │
│  │    │    - data: both × 2.0                       │        │   │
│  │    │                                              │        │   │
│  │    │ 3. Stars 규모 스케일링                       │        │   │
│  │    │    - >50k stars: × 4                        │        │   │
│  │    │    - >10k stars: × 2                        │        │   │
│  │    │    - >1k stars: × 1.5                       │        │   │
│  │    │                                              │        │   │
│  │    │ - FastAPI 결과: 4 CPU, 8GB                  │        │   │
│  │    └─────────────────────────────────────────────┘        │   │
│  │                                                            │   │
│  │ 3. MCPContext 자동 생성                                    │   │
│  │    {                                                       │   │
│  │      "service_type": "api",        // 자동 추론            │   │
│  │      "current_users": 12483,       // 자동 계산            │   │
│  │      "time_slot": "peak",          // 자동 감지            │   │
│  │      "cpu": 4,                     // 자동 추정            │   │
│  │      "memory": 8192,               // 자동 추정            │   │
│  │      "runtime_env": "prod"         // 기본값              │   │
│  │    }                                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  [자동화 단계 2: MCP Core 호출]                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ HTTP POST http://localhost:8000/plans                     │   │
│  │ {                                                          │   │
│  │   "github_url": "fastapi/fastapi",                        │   │
│  │   "metric_name": "total_events",                          │   │
│  │   "context": { ... }  // 위에서 생성한 MCPContext         │   │
│  │ }                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP Request
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                 app/routes/plans.py                              │
│                   (MCP Core FastAPI)                             │
│                                                                   │
│  [자동화 단계 3: 예측 파이프라인]                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 1. Context 검증 (자동)                                     │   │
│  │    - extract_context() → MCPContext 파싱                  │   │
│  │    - 필수 필드 자동 체크                                   │   │
│  │    - 실패 시 400 에러 + 명확한 메시지                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 2. 라우팅 (자동)                                           │   │
│  │    - select_route(ctx)                                    │   │
│  │    - 규칙:                                                 │   │
│  │      * prod + (api|web) → "lstm-v1"                       │   │
│  │      * dev → "baseline-simple"                            │   │
│  │      * data → "lstm-data-v1"                              │   │
│  │    - 결과: model_version = "lstm-v1"                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 3. 예측 엔진 실행 (자동)                                   │   │
│  │    ┌────────────────────────────────────────────┐        │   │
│  │    │ LSTMPredictor.run()                        │        │   │
│  │    │                                             │        │   │
│  │    │ 3.1 데이터 소스 자동 로드                   │        │   │
│  │    │     - factory.get_data_source()            │        │   │
│  │    │     - CSV 또는 MySQL (환경변수 기반)        │        │   │
│  │    │     - 최근 24시간 데이터 자동 쿼리          │        │   │
│  │    │                                             │        │   │
│  │    │ 3.2 모델 자동 로드                          │        │   │
│  │    │     - best_mcp_lstm_model.h5               │        │   │
│  │    │     - mcp_model_metadata.pkl               │        │   │
│  │    │       (scaler, features, seq_length)       │        │   │
│  │    │                                             │        │   │
│  │    │ 3.3 전처리 자동 실행                        │        │   │
│  │    │     - Feature 정렬 (메타데이터 순서)        │        │   │
│  │    │     - Scaler transform                     │        │   │
│  │    │     - Sequence 생성 (24 timesteps)         │        │   │
│  │    │                                             │        │   │
│  │    │ 3.4 예측 실행                               │        │   │
│  │    │     - model.predict()                      │        │   │
│  │    │     - 24개 포인트 생성 (시간별 예측)        │        │   │
│  │    │                                             │        │   │
│  │    │ 3.5 후처리                                  │        │   │
│  │    │     - Inverse transform (정규화 복원)      │        │   │
│  │    │     - Timestamp 자동 생성                   │        │   │
│  │    └────────────────────────────────────────────┘        │   │
│  │                                                            │   │
│  │    ⚠️ LSTM 실패 시 자동 폴백                               │   │
│  │    ┌────────────────────────────────────────────┐        │   │
│  │    │ BaselinePredictor.run()                    │        │   │
│  │    │ - 최근 데이터 통계 기반 예측                │        │   │
│  │    │ - 항상 성공 보장                            │        │   │
│  │    └────────────────────────────────────────────┘        │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 4. 정책 후처리 (자동)                                      │   │
│  │    ┌────────────────────────────────────────────┐        │   │
│  │    │ policy.postprocess_predictions()           │        │   │
│  │    │                                             │        │   │
│  │    │ 4.1 가중치 적용                             │        │   │
│  │    │     - context.weight 기반                  │        │   │
│  │    │     - peak time → × 1.2                    │        │   │
│  │    │     - low time → × 0.8                     │        │   │
│  │    │                                             │        │   │
│  │    │ 4.2 클램핑                                  │        │   │
│  │    │     - 0 ~ 1 범위로 제한                     │        │   │
│  │    │                                             │        │   │
│  │    │ 4.3 이상탐지 자동 실행                      │        │   │
│  │    │     ┌──────────────────────────────┐       │        │   │
│  │    │     │ anomaly.detect_anomaly()     │       │        │   │
│  │    │     │ - 최근 168시간 데이터 로드    │       │        │   │
│  │    │     │ - Z-score 자동 계산           │       │        │   │
│  │    │     │ - 임계값 비교 (기본 3.0)      │       │        │   │
│  │    │     │                               │       │        │   │
│  │    │     │ if Z-score > 3.0:            │       │        │   │
│  │    │     │   is_anomaly = True          │       │        │   │
│  │    │     │   severity = "high"/"medium" │       │        │   │
│  │    │     └──────────────────────────────┘       │        │   │
│  │    │                                             │        │   │
│  │    │ 4.4 Discord 알림 (자동, 비차단)             │        │   │
│  │    │     if is_anomaly and WEBHOOK_URL:         │        │   │
│  │    │       Thread(discord_alert.send()).start() │        │   │
│  │    │       (예측 응답은 대기 없이 즉시 반환)      │        │   │
│  │    └────────────────────────────────────────────┘        │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 5. 추천 계산 (자동)                                        │   │
│  │    max_pred = max(predictions)                            │   │
│  │                                                            │   │
│  │    if max_pred > 0.9:                                     │   │
│  │      recommended_flavor = "large"                         │   │
│  │      cost_per_day = 5.50                                  │   │
│  │    elif max_pred > 0.7:                                   │   │
│  │      recommended_flavor = "medium"                        │   │
│  │      cost_per_day = 2.80                                  │   │
│  │    else:                                                   │   │
│  │      recommended_flavor = "small"                         │   │
│  │      cost_per_day = 1.20                                  │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 6. DB 저장 (자동, 선택적)                                  │   │
│  │    - record_plan_request()                                │   │
│  │    - record_prediction()                                  │   │
│  │    - record_anomaly() (이상 시)                           │   │
│  │    - record_alert() (알림 전송 시)                        │   │
│  │    ⚠️ DB 실패 시 로그만 남기고 계속 진행                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  [응답 생성]                                                      │
│  PlansResponse {                                                 │
│    prediction: {                                                 │
│      predictions: [24 points],                                   │
│      is_anomaly: false,                                          │
│      generated_at: "2025-11-12T..."                              │
│    },                                                             │
│    recommended_flavor: "medium",                                 │
│    expected_cost_per_day: 2.80,                                  │
│    notes: "Automatic analysis"                                   │
│  }                                                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP 200 OK (JSON)
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│            mcp_analyzer/server_production.py                     │
│                                                                   │
│  [자동화 단계 4: 결과 포매팅]                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ - 응답 JSON 파싱                                           │   │
│  │ - Markdown 텍스트 자동 생성                                │   │
│  │ - 그래프/테이블 렌더링 (Claude Desktop 표시용)             │   │
│  └──────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ MCP Response (TextContent)
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                      사용자 (Claude Desktop)                      │
│                                                                   │
│  📊 분석 완료!                                                    │
│                                                                   │
│  **저장소**: fastapi/fastapi                                     │
│  **언어**: Python                                                 │
│  **Stars**: 91,825 ⭐                                             │
│                                                                   │
│  **추정 사용자**: 12,483명                                        │
│  **서비스 타입**: API                                             │
│  **활성 시간대**: Peak                                            │
│                                                                   │
│  **추천 인스턴스**: medium                                        │
│  **스펙**: 4 vCPU, 8GB RAM                                        │
│  **예상 비용**: $2.80/day ($84/month)                            │
│                                                                   │
│  **24시간 예측**:                                                 │
│  [그래프 또는 테이블]                                             │
│                                                                   │
│  **이상 징후**: 없음 ✅                                            │
└───────────────────────────────────────────────────────────────────┘
```

### 2.2 완전 자동화 핵심 포인트

**사용자가 제공하는 것**: 오직 GitHub URL만
**시스템이 자동으로 하는 것**:
1. ✅ 서비스 타입 추론 (web/api/worker/data)
2. ✅ 활성 시간대 감지 (peak/normal/low)
3. ✅ 사용자 수 계산 (Stars/Forks 기반)
4. ✅ CPU/Memory 추정 (다단계 로직)
5. ✅ MCPContext 생성 (필요한 모든 필드)
6. ✅ 모델 선택 (라우팅 규칙 기반)
7. ✅ 데이터 로드 및 전처리
8. ✅ 예측 실행 (LSTM → 실패 시 Baseline)
9. ✅ 이상탐지 및 알림
10. ✅ 비용/스펙 추천
11. ✅ 결과 포매팅 및 반환

**사용자가 절대 신경쓰지 않는 것**:
- 어떤 모델이 사용되는지
- 어떤 파라미터가 필요한지
- 데이터가 어디서 오는지
- 스케일러/정규화 같은 ML 디테일
- DB 저장 여부
- 이상탐지 로직

---

## 3. 디렉터리 구조 및 역할

### 3.1 MCP 연동 레이어
```
mcp_analyzer/
├── server_production.py    ⭐ Claude Desktop MCP 서버
│                             - GitHub 자동 분석
│                             - MCPContext 자동 생성
│                             - /plans 호출 및 결과 포매팅
├── requirements.txt         - mcp, PyGithub, requests
├── README.md               - MCP 서버 사용법
└── USAGE.md                - 도구 상세 가이드
```

### 3.2 예측 코어 (MVP)
```
app/
├── main.py                  - FastAPI 앱 초기화
├── routes/
│   └── plans.py            ⭐ /plans 엔드포인트 (계약)
├── core/
│   ├── context_extractor.py - Context 검증
│   ├── router.py            - 모델 자동 선택
│   ├── policy.py            - 후처리 + 이상탐지 트리거
│   ├── anomaly.py           - Z-score 이상탐지
│   ├── db.py                - DB 저장 (선택적)
│   ├── alerts/
│   │   └── discord_alert.py - Discord 웹훅 알림
│   └── predictor/
│       ├── lstm_predictor.py      - LSTM 예측 엔진
│       ├── baseline_predictor.py  - 폴백 예측 엔진
│       └── data_sources/
│           ├── factory.py         - 데이터 소스 자동 선택
│           ├── csv_source.py      - CSV 로드
│           └── mysql_source.py    - MySQL 쿼리
└── models/
    ├── plans.py             ⭐ PlansRequest/Response (계약)
    └── common.py            - MCPContext, PredictionResult
```

### 3.3 학습 모델 및 데이터
```
models/
├── best_mcp_lstm_model.h5       - 학습된 LSTM 모델
├── mcp_model_metadata.pkl       - Scaler + Feature 정보
└── training_history.json        - 학습 히스토리

data/
└── lstm_ready_cluster_data.csv  - 서빙용 데이터
                                   (1,439행 × 84컬럼)
```

### 3.4 인프라
```
docker-compose.yml    - MySQL + App (단순화)
Dockerfile           - Python 3.11 + 의존성
.env.example         - 환경 변수 템플릿
requirements.txt     - FastAPI, TF, PyGithub 등
```

---

## 4. 자동화 시나리오

### 4.1 시나리오 A: 일반 사용자 (Claude Desktop)

**사용자 행동**:
```
@github-analyzer analyze https://github.com/fastapi/fastapi
```

**시스템 자동 처리**:
1. GitHub API로 메타데이터 수집 (자동)
2. 서비스 타입 추론: Python + "api"/"rest" 키워드 → "api" (자동)
3. 사용자 수 계산: 91,825 × 0.05 + 8,212 × 1.5 = 12,483명 (자동)
4. CPU/Memory 추정: Python(2/4GB) × api(1.2) × >50k stars(4) = 4 CPU, 8GB (자동)
5. MCPContext 생성 (자동)
6. /plans 호출 (자동)
7. LSTM 예측 실행 (자동)
8. 이상탐지 (자동)
9. 추천 계산: max=0.65 → "medium", $2.80/day (자동)
10. 결과 반환 (자동)

**사용자가 본 것**: 최종 결과만

---

### 4.2 시나리오 B: 이상 징후 감지

**사용자 행동**:
```
@github-analyzer analyze https://github.com/some/repo
```

**시스템 자동 처리**:
1. ~ 9. (동일)
10. 이상탐지: Z-score = 5.2 > 3.0 → 이상 감지 (자동)
11. Discord 알림 전송 (비동기, 자동)
    ```
    ⚠️ 이상 징후 감지!
    저장소: some/repo
    Z-score: 5.2
    예측 최댓값: 0.98 (정상 범위 0.72 ± 0.08)
    ```
12. 응답에 이상 정보 포함 (자동)
    ```
    ⚠️ 이상 징후: 예측값이 히스토리 대비 비정상적으로 높습니다.
    권장: 배포 전 추가 검토 필요
    ```

**사용자가 본 것**: 경고 메시지 포함된 결과

---

### 4.3 시나리오 C: LSTM 실패 시 자동 폴백

**상황**: 모델 파일 없음 or 데이터 부족 or TF 오류

**시스템 자동 처리**:
1. LSTM 예측 시도 → 예외 발생
2. 자동 catch 및 로그 기록
3. Baseline 예측기로 자동 전환 (무중단)
4. 통계 기반 예측 실행
5. 응답에 "Baseline 모드" 표시
   ```
   ℹ️ 참고: 통계 기반 예측 사용 (LSTM 일시 불가)
   ```

**사용자가 본 것**: 정상 결과 (모델 전환 사실 알 수 있지만 서비스는 중단 없음)

---

## 5. API 계약 (Contract)

### 5.1 /plans 엔드포인트

**절대 변경 금지**: 프론트엔드/CI/CD가 의존하는 계약

#### 요청 스키마
```python
class PlansRequest(BaseModel):
    github_url: str           # 예: "fastapi/fastapi"
    metric_name: str          # 예: "total_events"
    context: MCPContext       # 자동 생성된 컨텍스트
```

#### MCPContext (자동 생성)
```python
class MCPContext(BaseModel):
    service_type: str         # "web" | "api" | "worker" | "data"
    current_users: int        # 추정 사용자 수
    time_slot: str            # "peak" | "normal" | "low" | "weekend"
    cpu: int                  # 추정 CPU (vCPU)
    memory: int               # 추정 Memory (MB)
    runtime_env: str = "prod" # "prod" | "dev"
    weight: float = 1.0       # 가중치 (기본값)
```

#### 응답 스키마
```python
class PlansResponse(BaseModel):
    prediction: PredictionResult
    recommended_flavor: str   # "small" | "medium" | "large"
    expected_cost_per_day: float
    generated_at: datetime
    notes: str
```

#### PredictionResult
```python
class PredictionResult(BaseModel):
    predictions: List[Prediction]  # 24개 포인트
    is_anomaly: bool
    anomaly_score: Optional[float]
    generated_at: datetime
    model_version: str
```

---

## 6. 데이터 파이프라인

### 6.1 자동 데이터 소스 선택

```python
# .env
DATA_SOURCE_BACKEND=csv  # 또는 mysql

# factory.py (자동 선택)
def get_data_source():
    backend = os.getenv("DATA_SOURCE_BACKEND", "csv")
    
    if backend == "mysql":
        return MySQLDataSource(
            host=os.getenv("MYSQL_HOST"),
            user=os.getenv("MYSQL_USER"),
            password=os.getenv("MYSQL_PASSWORD"),
            database=os.getenv("MYSQL_DATABASE")
        )
    else:
        return CSVDataSource(
            path=os.getenv("CSV_DATA_PATH", "data/lstm_ready_cluster_data.csv")
        )
```

**사용자 행동 불필요**: 환경 변수만 설정하면 자동 선택

---

### 6.2 자동 전처리 파이프라인

```python
# LSTMPredictor.run() 내부 (모두 자동)
def run(self, github_url, metric_name, ctx, model_version):
    # 1. 데이터 로드 (자동)
    data_source = factory.get_data_source()
    df = data_source.load_history(github_url, metric_name, hours=24)
    
    # 2. Feature 정렬 (자동)
    # 메타데이터의 feature_names 순서대로 정렬
    df = df[self.metadata["feature_names"]]
    
    # 3. Scaling (자동)
    scaled = self.scaler.transform(df)
    
    # 4. Sequence 생성 (자동)
    X = create_sequences(scaled, seq_length=24)
    
    # 5. 예측 (자동)
    y_pred = self.model.predict(X)
    
    # 6. Inverse transform (자동)
    y_original = self.target_scaler.inverse_transform(y_pred)
    
    return PredictionResult(...)
```

**사용자 개입 불필요**: 모든 ML 파이프라인 자동 실행

---

## 7. 예외 처리 및 폴백

### 7.1 예외 처리 계층

```
┌─────────────────────────────────────┐
│ Level 1: GitHub API 실패             │
│ → MCP 서버가 명확한 에러 메시지 반환   │
│   "저장소를 찾을 수 없거나 비공개입니다"│
└─────────────────────────────────────┘
           ↓ (통과 시)
┌─────────────────────────────────────┐
│ Level 2: Context 검증 실패            │
│ → 400 Bad Request + 상세 메시지       │
│   "필수 필드 'service_type' 누락"     │
└─────────────────────────────────────┘
           ↓ (통과 시)
┌─────────────────────────────────────┐
│ Level 3: LSTM 예측 실패               │
│ → 자동 Baseline 폴백 (무중단)         │
│   로그: "LSTM failed, using baseline"│
└─────────────────────────────────────┘
           ↓ (통과 시)
┌─────────────────────────────────────┐
│ Level 4: DB 저장 실패                 │
│ → 로그만 남기고 계속 진행              │
│   예측 파이프라인 영향 없음             │
└─────────────────────────────────────┘
           ↓ (통과 시)
┌─────────────────────────────────────┐
│ Level 5: Discord 알림 실패            │
│ → 로그만 남기고 계속 진행              │
│   응답 생성에 영향 없음                │
└─────────────────────────────────────┘
```

### 7.2 폴백 우선순위

1. **LSTM → Baseline**: 항상 자동 폴백
2. **MySQL → CSV**: 설정 변경으로 전환 가능
3. **DB 저장**: 실패해도 예측 계속
4. **Discord 알림**: 실패해도 응답 반환

**핵심 원칙**: 예측 파이프라인은 절대 중단되지 않음

---

## 8. 배포 및 실행

### 8.1 로컬 실행 (개발/테스트)

```powershell
# 1. 가상환경 활성화
.\.venv\Scripts\Activate.ps1

# 2. MCP Core 실행
python -m uvicorn app.main:app --reload --port 8000

# 3. Claude Desktop 설정
# $env:APPDATA\Claude\claude_desktop_config.json
{
  "mcpServers": {
    "github-analyzer": {
      "command": "C:\\...\\python.exe",
      "args": ["C:\\...\\mcp_analyzer\\server_production.py"],
      "env": {
        "MCP_CORE_URL": "http://localhost:8000"
      }
    }
  }
}

# 4. Claude Desktop 재시작
# 5. @github-analyzer 사용 가능
```

### 8.2 Docker 실행 (프로덕션)

```bash
# docker-compose.yml (MySQL + App)
docker-compose up -d --build

# 헬스 체크
curl http://localhost:8000/health

# Swagger UI
# http://localhost:8000/docs
```

### 8.3 환경 변수 설정

```bash
# .env (git에 커밋 금지)
DATA_SOURCE_BACKEND=csv
CSV_DATA_PATH=data/lstm_ready_cluster_data.csv

DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
DISCORD_BOT_NAME=MCP-Alerts
ANOMALY_Z=3.0

# MySQL 사용 시
# DATA_SOURCE_BACKEND=mysql
# MYSQL_HOST=localhost
# MYSQL_USER=mcp_user
# MYSQL_PASSWORD=secret
# MYSQL_DATABASE=mcp_core
```

---

## 9. 핵심 자동화 요약

### 사용자 관점
```
입력: GitHub URL
클릭: 없음
대기: 3~5초
출력: 완전한 분석 결과 + 추천 + 비용
```

### 시스템 관점
```
10단계 자동화:
1. GitHub 메타데이터 수집
2. 서비스 타입 추론
3. 사용자 수 계산
4. 리소스 추정
5. MCPContext 생성
6. 모델 자동 선택
7. 데이터 로드 및 예측
8. 이상탐지 및 알림
9. 추천 계산
10. 결과 반환

사용자 개입: 0회
시스템 자율성: 100%
```

---

## 10. feat/#9/openstack 브랜치 병합 고려사항

### 브랜치 주요 변경사항
- `app/core/openstack/` 디렉터리 추가 (배포 어댑터)
- `context_extractor.py` 오타 수정 (**이미 현재 브랜치에서 수정 완료**)

### 병합 전략
1. **배포 로직은 /plans와 분리 유지**
   - OpenStack 어댑터는 `/plans` 응답을 소비하는 별도 레이어
   - 예측 코어와 강하게 결합하지 않음

2. **계약 유지 필수**
   - `PlansRequest`/`PlansResponse` 스키마 변경 없음
   - 예측 파이프라인 로직 변경 없음

3. **선택적 통합**
   - OpenStack 배포는 MVP 범위 밖
   - 필요 시 별도 서비스로 분리 권장

### 권장 병합 순서
```bash
# 1. 현재 브랜치 정리 (완료)
# 2. feat/#9/openstack에서 배포 어댑터만 체리픽
git cherry-pick <openstack-adapter-commit>

# 3. 충돌 해결 (context_extractor 이미 수정됨)
# 4. 테스트
python tests/smoke_check.py

# 5. 병합
git merge feat/#9/openstack
```

---

## 부록: NLP 기반 고정 프롬프트 패턴

### 사용자가 사용할 표준 문구
```
1. "@github-analyzer analyze <URL>"
2. "@github-analyzer estimate-resources <URL>"
3. "이 레포 분석해줘: <URL>"
4. "<URL> 리소스 예측해줘"
```

### MCP 서버가 인식하는 패턴
- URL 추출 정규식: `https?://github\.com/[\w\-]+/[\w\-]+`
- 도구 자동 선택:
  - "analyze" → `analyze-github-repo` (전체 예측)
  - "estimate" → `estimate-resources` (빠른 추정)
- 컨텍스트 자동 생성: URL만 있으면 OK

**핵심**: 사용자는 자연어로 요청, 시스템은 자동 파싱 및 실행

---

**작성일**: 2025-11-12  
**버전**: 1.0 (MVP 기준)  
**유지보수**: 계약 변경 시 이 문서 필수 업데이트
